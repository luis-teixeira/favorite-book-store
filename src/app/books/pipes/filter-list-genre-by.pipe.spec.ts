/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterListGenreByPipe } from './filter-list-genre-by.pipe';

describe('FilterListGenreByPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterListGenreByPipe();
    expect(pipe).toBeTruthy();
  });
});
